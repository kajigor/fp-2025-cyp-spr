module List where

-- Generates all permutations of the given list. 
perms :: [a] -> [[a]]
perms xs = undefined 

-- Concatenate the shortest prefix of xss whose total sum is positive. 
-- If no sum is positive, then the whole list is concatenated.
collapse :: [[Int]] -> [Int]
collapse xss = undefined 
